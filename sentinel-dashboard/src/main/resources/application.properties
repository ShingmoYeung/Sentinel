# startup port
server.port=8080

# spring settings
spring.http.encoding.force=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true

# cookie name setting
server.servlet.session.cookie.name=sentinel_dashboard_cookie

# logging settings
logging.level.org.springframework.web=INFO
logging.file=${user.home}/logs/csp/sentinel-dashboard.log
logging.pattern.file= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
#logging.pattern.console= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# auth settings
auth.filter.exclude-urls=/,/auth/login,/auth/logout,/registry/machine,/version
auth.filter.exclude-url-suffixes=htm,html,js,css,map,ico,ttf,woff,png
# If auth.enabled=false, Sentinel console disable login
auth.username=${sentinel-auth-username:sentinel}
auth.password=${sentinel-auth-password:sentinel}

# Inject the dashboard version. It's required to enable
# filtering in pom.xml for this resource file.
sentinel.dashboard.version=@project.version@

# Nacos Datasource Configuration
sentinel.datasource.nacos.server-address=${nacos-service-addr:middleware-two.bcc-szth.baidu.com:8848}
sentinel.datasource.nacos.namespace=${sentinel-nacos-service-namespace:v2x-platform-stand-sentinel-rule-prod}
#sentinel.datasource.nacos.namespace=${sentinel-nacos-service-namespace:v2x-platform-apigateway-sentinel-rule-prod}
sentinel.datasource.nacos.username=
sentinel.datasource.nacos.password=